% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_weights.R
\name{plot_weights}
\alias{plot_weights}
\title{Plot Cumulative Weights Heatmap}
\usage{
plot_weights(dataset, cw1 = 0.95, cw2 = 0.5, cw3 = 0.25)
}
\arguments{
\item{dataset}{Output from run_slidingwin (either a data frame or a list with 'dataset' item) containing columns:
Start_Day, End_Day, AIC, and ModWeight}

\item{cw1}{A numeric value between 0 and 1 defining the cumulative weight threshold. Defaults to 0.95.}
}
\value{
A ggplot object showing the cumulative weights heatmap
}
\description{
This function creates a heatmap visualization showing cumulative model weights.
It identifies models within a specified cumulative weight threshold and plots
them using geom_tile with cumulative weights as the color.
}
\examples{
# Example usage:
Climate <- read.csv(system.file("MassClimate.csv", package = "climwin"))
Mass <- read.csv(system.file("Mass.csv", package = "climwin"))
results <- run_slidingwin(range = 0:100, 
                        climate_data = Climate, 
                        bio_data = Mass,
                        basemodel = lm(Mass ~ climate, data = bio_data))
plot_weights(results, cw1 = 0.95)

}
