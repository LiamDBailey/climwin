% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/process_data.R
\name{process_data}
\alias{process_data}
\title{Process Climate and Biological Data for Sliding Window Analysis}
\usage{
process_data(
  climate_data,
  bio_data,
  range,
  cdate = "Date",
  bdate = "Date",
  xvar = "Temp",
  spatial = NULL,
  type = "relative",
  refday = NULL,
  cohort = NULL,
  cinterval = "day"
)
}
\arguments{
\item{climate_data}{A data frame containing climate data}

\item{bio_data}{A data frame containing biological data}

\item{range}{A numeric vector specifying the number of days to look back}

\item{cdate}{Character string specifying the name of the date column in climate_data. Defaults to "Date".}

\item{bdate}{Character string specifying the name of the date column in bio_data. Defaults to "Date".}

\item{xvar}{Character string specifying the name of the climate variable column. Defaults to "Temp".}

\item{spatial}{Character string specifying the name of the spatial grouping column. Defaults to NULL.}

\item{type}{Character string specifying the type of date range calculation. Must be either "relative" (default) or "absolute".}

\item{refday}{Character string specifying the reference date for absolute type. Defaults to NULL.}

\item{cohort}{Character string specifying the name of the cohort column. Defaults to NULL.}

\item{cinterval}{Character string specifying the temporal resolution. Must be "day" (default),
"week", or "month". When "month" or "week", \code{climate_data} must already be
pre-aggregated to one row per period — use \code{\link{trans_clim_interval}} first.}
}
\value{
A list containing:
        - bio_data: The processed biological data with date_int column
        - bio_int_ranges: List of integer date ranges for each spatial group
        - bio_data_row: Vector of row indices for rejoining data
        - bio_xvar_ranges: Matrix of climate variable values for each range
        - max_possible_range: Maximum possible range based on climate data
}
\description{
This internal function processes climate and biological data to prepare them for sliding window analysis.
It handles spatial grouping, date conversion, and range validation.
}
\keyword{internal}
