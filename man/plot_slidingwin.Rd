% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_slidingwin.R
\name{plot_slidingwin}
\alias{plot_slidingwin}
\title{Plot Sliding Window Analysis Results}
\usage{
plot_slidingwin(
  dataset,
  cw1 = 0.95,
  plots = c("delta", "weights", "windows", "best"),
  ...
)
}
\arguments{
\item{dataset}{Output from run_slidingwin (a list with 'dataset' and 'bestModel' items)}

\item{cw1}{A numeric value between 0 and 1 defining the cumulative weight threshold. Defaults to 0.95.}

\item{plots}{A character vector specifying which plots to include. Can include any of:
'delta', 'weights', 'windows', 'best'. Defaults to c('delta', 'weights', 'windows', 'best').}

\item{.}{... Additional arguments passed to `plot_best` to define model predictions.}
}
\value{
A patchwork object combining the specified plots
}
\description{
This function creates a combined visualization of sliding window analysis results
by combining multiple plot types using patchwork. Available plot types include
'delta', 'weights', 'windows', and 'best'.
}
\examples{
# Example usage:
data("MassClimate")
data("Mass")
results <- run_slidingwin(range = 0:100, 
                        climate_data = MassClimate, 
                        bio_data = Mass,
                        basemodel = lm(Mass ~ climate, data = bio_data))

# Create combined plot with all plots (default)
plot_slidingwin(results)

# Create combined plot with specific plots
plot_slidingwin(results, plots = c('weights', 'windows'))

}
