% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_weightwin_n.R
\name{run_weightwin_n}
\alias{run_weightwin_n}
\title{Run Multiple Weighted Climate Window Optimizations}
\usage{
run_weightwin_n(
  n,
  range,
  bio_data,
  climate_data,
  basemodel,
  cdate = "Date",
  bdate = "Date",
  xvar = "Temp",
  method = "L-BFGS-B",
  lower = c(0.1, 0.1),
  upper = c(10, 1000),
  control = list(maxit = 100),
  par_min = 0.1,
  par_max = 10
)
}
\arguments{
\item{n}{Integer specifying the number of iterations to run. Required.}

\item{range}{A numeric vector specifying the number of days to look back from each date in bio_data.
For example, 0 represents the date itself, while 100 represents 100 days before that date.}

\item{bio_data}{A data frame containing biological data with a date column. Required.}

\item{climate_data}{A data frame containing climate data. Required.}

\item{basemodel}{An lm model object that will be updated for each set of weights (e.g., lm(Mass ~ climate, data = bio_data)). Required.}

\item{cdate}{Character string specifying the name of the date column in climate_data. Defaults to "Date".}

\item{bdate}{Character string specifying the name of the date column in bio_data. Defaults to "Date".}

\item{xvar}{Character string specifying the name of the climate variable column in climate_data. Defaults to "Temp".}

\item{method}{The optimization method to use. Defaults to "L-BFGS-B".}

\item{lower}{Lower bounds for the parameters. Defaults to c(0.1, 0.1).}

\item{upper}{Upper bounds for the parameters. Defaults to c(10, 1000).}

\item{control}{Additional control parameters for optim. Defaults to list(maxit = 100).}

\item{par_min}{Minimum value for random parameter generation. Defaults to 0.1.}

\item{par_max}{Maximum value for random parameter generation. Defaults to 10.}
}
\value{
A data frame containing the top model results for each iteration with columns:
        - iteration: The iteration number (1 to n)
        - shape: The optimal shape parameter found
        - scale: The optimal scale parameter found
        - AIC: The AIC value of the best model
        - convergence: Convergence code from optim
        - model: The best model object for this iteration
        - data: The optimal data used for this model
}
\description{
This function runs the weighted climate window optimization multiple times with
randomly generated Weibull parameters. For each iteration, it generates random
shape and scale parameters from a uniform distribution and calls run_weightwin
to find the optimal model. Returns a dataframe with the top model results for
each iteration.
}
\examples{
# Example usage:
Climate <- read.csv(system.file("MassClimate.csv", package = "climwin"))
Mass <- read.csv(system.file("Mass.csv", package = "climwin"))

results <- run_weightwin_n(n = 5,
                          range = 0:100, 
                          bio_data = Mass,
                          climate_data = Climate,
                          cdate = "Date", 
                          bdate = "Date",
                          xvar = "Temp",
                          basemodel = lm(Mass ~ climate, data = bio_data))
                          
# View the results
print(results)

}
